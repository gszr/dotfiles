# vim: filetype=zsh
# dynamic-colors setup

if [ -d ~/.dynamic-colors ]; then
  path_prepend "$HOME/.dynamic-colors/bin"
  source $HOME/.dynamic-colors/completions/dynamic-colors.zsh
  dynamic-colors init
  function cycle { dynamic-colors cycle }
  zle -N cycle
  bindkey "^B" cycle
fi
